import { LoginForm } from '../components/LoginForm/LoginForm';

const LoginPage = () => {
  return (
    <main>
      <title>Login</title>
      <LoginForm />
    </main>
  );
};

export default LoginPage;
